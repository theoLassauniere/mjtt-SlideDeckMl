{
    "name": "mjtt-slide-deck-ml",
    "name": "mjtt-slide-deck-ml",
    "description": "The extension specific package",
    "version": "0.0.1",
    "displayName": "sdml",
    "type": "module",
    "repository": "https://github.com/theoLassauniere/mjtt-SlideDeckMl",
    "engines": {
        "vscode": "^1.67.0"
    },
    "categories": [
        "Programming Languages"
    ],
    "contributes": {
        "languages": [{
            "id": "slide-deck-ml",
            "aliases": ["SlideDeckML", "slide-deck-ml"],
            "extensions": [".sdml"],
            "configuration": "./language-configuration.json"
        }],
        "grammars": [{
            "language": "slide-deck-ml",
            "scopeName": "source.slide-deck-ml",
            "path": "syntaxes/slide-deck-ml.tmLanguage.json"
        }]
    },
    "activationEvents": [
        "onLanguage:slide-deck-ml"
    ],
    "main": "./out/extension/main.cjs.js",
    "scripts": {
        "clean": "shx rm -fr *.tsbuildinfo out syntaxes",
        "vscode:prepublish": "npm run build",
        "build:prepare": "shx mkdir -p ./syntaxes/ && shx cp -f ../language/syntaxes/slide-deck-ml.tmLanguage.json ./syntaxes/slide-deck-ml.tmLanguage.json",
        "build": "npm run build:prepare && tsc -b tsconfig.json && node esbuild.mjs",
        "build:clean": "npm run clean && npm run build",
        "watch": "npm run build:prepare && concurrently -n tsc,esbuild -c blue,yellow \"tsc -b tsconfig.json --watch\" \"node esbuild.mjs --watch\"",
        "package": "vsce package --out ../../"
    },
    "dependencies": {
        "slide-deck-ml-language": "0.0.1",
        "slide-deck-ml-cli": "0.0.1",
        "vscode-languageclient": "~9.0.1",
        "vscode-languageserver": "~9.0.1"
    },
    "devDependencies": {
        "@types/vscode": "~1.67.0",
        "concurrently": "~8.2.1",
        "esbuild": "~0.25.5",
        "vsce": "^2.15.0"
    },
    "vsce": {
      "dependencies": false
    }
}
