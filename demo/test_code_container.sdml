Presentation CodeEditorTest {
    Template {
        logos = [
            logo "../assets/logo_company.png" TOP LEFT size 100
            logo "../assets/logo_school.svg" TOP RIGHT size 120 80
        ]
        companyName = "Langium Corporation"
        fontName = "Roboto"
        fontSize = "1rem"
        fontColor = "#222222"
        titlesSize = "2rem"
        backgroundColor = "white"
    }

    Slide {
        title = "Ceci est une diapositive de code"
        code Python
            ```
n = 3
print("Je vais vous demander", n, "nombres")
for i in range(n):
    x = int(input("Donnez un nombre : "))
    if x > 0:
        print(x, "est positif")
    else:
        print(x, "est négatif ou nul")
print("Fin")
            ```
    }
    Slide {
        title = "Ceci est une seconde diapositive de code"
        code Javascript
            ```
// Basic Javascript code
let n = 6;
n += 2; 
console.log("Final n value : ", n);
            ```
        text "Test"
    }
}

